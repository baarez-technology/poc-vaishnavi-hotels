/**
 * Staff Data
 * Empty - data comes from API
 */

export const staffData = [];

/**
 * Get unique roles from staff data
 * Returns empty array when data comes from API
 */
export function getRoles(): string[] {
  return [];
}
